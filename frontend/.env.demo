# Vite environment variables available in the browser must start with VITE_

# API base URL
VITE_API_BASE=http://localhost:5000/api

# Socket server URL
VITE_SOCKET_URL=http://localhost:5000

# Optional: cloudinary presets or upload preset name if used on client
# VITE_CLOUDINARY_UPLOAD_PRESET=unsigned_upload_preset
